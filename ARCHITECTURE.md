# 🏗️ BantuBuzz - System Architecture

> **Visual guide to understanding how BantuBuzz works**

---

## 🎯 High-Level Architecture

```
┌─────────────────────────────────────────────────────────┐
│                     USER BROWSER                         │
│                  http://localhost:3000                   │
└────────────────────┬────────────────────────────────────┘
                     │
                     │ HTTP/WebSocket
                     ▼
┌─────────────────────────────────────────────────────────┐
│               REACT FRONTEND (Vite)                      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │   Pages      │  │  Components  │  │    Hooks     │  │
│  │  - Home      │  │  - Navbar    │  │  - useAuth   │  │
│  │  - Login     │  │  - Footer    │  │              │  │
│  │  - Register  │  │              │  │              │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
│         │                                    │           │
│         │              API Client (Axios)     │           │
│         └────────────────┬──────────────────┘           │
└──────────────────────────┼──────────────────────────────┘
                           │
                           │ REST API / JWT
                           ▼
┌─────────────────────────────────────────────────────────┐
│               FLASK BACKEND (Python)                     │
│                http://localhost:5000                     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │   Routes     │  │   Services   │  │    Models    │  │
│  │  - auth.py   │  │  - email     │  │  - User      │  │
│  │  - users.py  │  │  - payment   │  │  - Creator   │  │
│  │  - creators  │  │              │  │  - Brand     │  │
│  │  - packages  │  │              │  │  - Package   │  │
│  │  - bookings  │  │              │  │  - etc...    │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
│                           │                              │
└───────────────────────────┼──────────────────────────────┘
                            │
                            │ SQLAlchemy ORM
                            ▼
┌─────────────────────────────────────────────────────────┐
│                  DATABASE (SQLite/PostgreSQL)            │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐          │
│  │Users │ │Creators│Brands│Packages│Bookings│  + 5 more│
│  └──────┘ └──────┘ └──────┘ └──────┘ └──────┘          │
└─────────────────────────────────────────────────────────┘
```

---

## 🔄 Request Flow

### User Registration Flow

```
┌─────────┐     ┌─────────┐     ┌─────────┐     ┌─────────┐
│ Browser │────▶│ React   │────▶│ Flask   │────▶│Database │
│         │     │ Form    │     │ /auth   │     │         │
└─────────┘     └─────────┘     └─────────┘     └─────────┘
    │               │                 │                │
    │ 1. Fill Form  │                 │                │
    │──────────────▶│                 │                │
    │               │ 2. POST /register│               │
    │               │─────────────────▶│                │
    │               │                 │ 3. Hash Password│
    │               │                 │ 4. Create User  │
    │               │                 │────────────────▶│
    │               │                 │                │
    │               │                 │◀───5. User ID───│
    │               │                 │ 6. Generate Token│
    │               │◀────7. Success──│                │
    │◀──8. Redirect─│                 │                │
    │  to Dashboard │                 │                │
    │               │                 │                │
```

### Authenticated API Request Flow

```
┌─────────┐     ┌─────────┐     ┌─────────┐     ┌─────────┐
│ Browser │────▶│ Axios   │────▶│ Flask   │────▶│Database │
│         │     │ Client  │     │ Route   │     │         │
└─────────┘     └─────────┘     └─────────┘     └─────────┘
    │               │                 │                │
    │ 1. API Call   │                 │                │
    │──────────────▶│                 │                │
    │               │ 2. Add JWT Token│                │
    │               │ 3. GET /api/xxx │                │
    │               │─────────────────▶│                │
    │               │                 │ 4. Verify Token│
    │               │                 │ 5. Query DB    │
    │               │                 │────────────────▶│
    │               │                 │◀───6. Data─────│
    │               │◀────7. JSON────│                │
    │◀──8. Update UI│                 │                │
```

---

## 📊 Database Schema (Simplified)

```
┌──────────────┐         ┌──────────────┐
│    Users     │         │    Messages  │
├──────────────┤         ├──────────────┤
│ id (PK)      │◀───┬───▶│ id (PK)      │
│ email        │    │    │ sender_id    │
│ password_hash│    │    │ receiver_id  │
│ user_type    │    │    │ content      │
└──────┬───────┘    │    └──────────────┘
       │            │
       │            │    ┌──────────────┐
       ├────────────────▶│Notifications │
       │            │    ├──────────────┤
       │            │    │ id (PK)      │
       │            │    │ user_id (FK) │
       ▼            │    │ message      │
┌──────────────┐   │    └──────────────┘
│CreatorProfile│   │
├──────────────┤   │
│ id (PK)      │   │
│ user_id (FK) │───┘
│ bio          │
│ categories   │
│ social_links │
└──────┬───────┘
       │
       │ has many
       ▼
┌──────────────┐         ┌──────────────┐
│   Packages   │◀────────│   Bookings   │
├──────────────┤         ├──────────────┤
│ id (PK)      │         │ id (PK)      │
│ creator_id   │         │ package_id   │
│ title        │         │ creator_id   │
│ price        │         │ brand_id     │
│ deliverables │         │ status       │
└──────────────┘         │ amount       │
                         └──────────────┘
                                │
                                │
┌──────────────┐               │
│BrandProfile  │◀──────────────┘
├──────────────┤
│ id (PK)      │         ┌──────────────┐
│ user_id (FK) │◀────────│  Campaigns   │
│ company_name │         ├──────────────┤
│ logo         │         │ id (PK)      │
│ industry     │         │ brand_id     │
└──────────────┘         │ title        │
                         │ budget       │
                         └──────────────┘
```

---

## 🔐 Authentication Flow

```
┌─────────────────────────────────────────────────────────┐
│                    LOGIN PROCESS                         │
└─────────────────────────────────────────────────────────┘

1. User enters email + password
   │
   ▼
2. POST /api/auth/login
   │
   ▼
3. Backend finds user by email
   │
   ▼
4. Check password hash
   │
   ├─── Invalid ────▶ Return 401 Error
   │
   └─── Valid
        │
        ▼
5. Generate JWT access token (1 hour)
   Generate JWT refresh token (30 days)
   │
   ▼
6. Return tokens + user data
   │
   ▼
7. Frontend stores:
   - access_token in localStorage
   - refresh_token in localStorage
   - user data in state
   │
   ▼
8. Redirect to dashboard


┌─────────────────────────────────────────────────────────┐
│               AUTHENTICATED REQUEST                      │
└─────────────────────────────────────────────────────────┘

1. User makes API request
   │
   ▼
2. Axios intercepts request
   Adds: Authorization: Bearer <access_token>
   │
   ▼
3. Backend receives request
   │
   ▼
4. JWT middleware validates token
   │
   ├─── Invalid/Expired ────▶ Return 401
   │                          │
   │                          ▼
   │                       Try refresh token
   │                          │
   │                          ▼
   │                       Return new access token
   │
   └─── Valid
        │
        ▼
5. Process request
   │
   ▼
6. Return data
```

---

## 📁 File Organization

```
Backend Structure:
==================

app/
├── __init__.py          ← App factory
├── config.py            ← Configuration classes
│
├── models/              ← Database Models (SQLAlchemy)
│   ├── __init__.py      ← Export all models
│   ├── user.py          ← User model
│   ├── creator_profile.py
│   ├── brand_profile.py
│   ├── package.py
│   ├── campaign.py
│   ├── booking.py
│   ├── message.py
│   ├── notification.py
│   ├── saved_creator.py
│   └── analytics.py
│
├── routes/              ← API Endpoints (Blueprints)
│   ├── __init__.py
│   ├── auth.py          ← /api/auth/*
│   ├── users.py         ← /api/users/*
│   ├── creators.py      ← /api/creators/*
│   ├── brands.py        ← /api/brands/*
│   ├── packages.py      ← /api/packages/*
│   ├── campaigns.py     ← /api/campaigns/*
│   ├── bookings.py      ← /api/bookings/*
│   ├── messages.py      ← /api/messages/*
│   ├── notifications.py ← /api/notifications/*
│   └── analytics.py     ← /api/analytics/*
│
├── services/            ← Business Logic
│   ├── __init__.py
│   ├── email_service.py ← Email handling
│   └── payment_service.py ← Paynow integration
│
└── utils/               ← Utilities
    └── __init__.py


Frontend Structure:
===================

src/
├── main.jsx             ← Entry point
├── App.jsx              ← Main router
├── index.css            ← Global styles
│
├── pages/               ← Page Components
│   ├── Home.jsx         ← Landing page
│   ├── Login.jsx
│   ├── RegisterCreator.jsx
│   ├── RegisterBrand.jsx
│   ├── CreatorDashboard.jsx
│   ├── BrandDashboard.jsx
│   ├── Creators.jsx
│   ├── CreatorProfile.jsx
│   ├── Packages.jsx
│   ├── PackageDetails.jsx
│   ├── Messages.jsx
│   └── NotFound.jsx
│
├── components/          ← Reusable Components
│   ├── Navbar.jsx
│   └── Footer.jsx
│
├── services/            ← API Integration
│   └── api.js           ← Axios client + all API calls
│
├── hooks/               ← Custom React Hooks
│   └── useAuth.jsx      ← Authentication context
│
└── utils/               ← Utility functions
```

---

## 🔌 API Integration Pattern

```javascript
┌──────────────────────────────────────────────────────────┐
│                  FRONTEND API CALL                       │
└──────────────────────────────────────────────────────────┘

// 1. User action (e.g., button click)
const handleLogin = async () => {

  // 2. Call API service
  const response = await authAPI.login(credentials);

  // 3. Axios intercepts (adds token if exists)
  //    → Authorization: Bearer <token>

  // 4. Flask receives request
  //    → Validates JWT if protected route

  // 5. Process & return data
  //    ← JSON response

  // 6. Update UI state
  setUser(response.data.user);

  // 7. Navigate
  navigate('/dashboard');
};
```

---

## 🎨 UI Component Hierarchy

```
App
├── BrowserRouter
    ├── QueryClientProvider
        ├── AuthProvider
            ├── Routes
                ├── Home
                │   ├── Navbar
                │   ├── Hero Section
                │   ├── Stats Section
                │   ├── Features Section
                │   ├── How It Works
                │   └── Footer
                │
                ├── Login
                │   ├── Navbar
                │   └── LoginForm
                │
                ├── RegisterCreator
                │   ├── Navbar
                │   └── RegistrationForm
                │
                ├── CreatorDashboard (Protected)
                │   ├── Navbar
                │   ├── StatsCards
                │   ├── BookingsList
                │   └── PackagesGrid
                │
                └── BrandDashboard (Protected)
                    ├── Navbar
                    ├── CampaignsGrid
                    ├── SavedCreators
                    └── AnalyticsCharts
```

---

## 🔄 State Management

```
┌────────────────────────────────────────────────────────┐
│              FRONTEND STATE LAYERS                     │
└────────────────────────────────────────────────────────┘

1. Local Component State (useState)
   └─▶ Form inputs, UI toggles, etc.

2. Auth Context (useAuth)
   └─▶ User, Profile, isAuthenticated
       ├─ login()
       ├─ logout()
       ├─ registerCreator()
       └─ registerBrand()

3. TanStack Query (React Query)
   └─▶ Server state caching
       ├─ Creators list
       ├─ Packages list
       ├─ Bookings
       └─ Auto refresh, caching

4. URL State (React Router)
   └─▶ Current route, params
```

---

## 📡 Real-time Features (SocketIO)

```
┌─────────────┐                    ┌─────────────┐
│   Client 1  │                    │   Client 2  │
│  (Browser)  │                    │  (Browser)  │
└──────┬──────┘                    └──────┬──────┘
       │                                  │
       │  WebSocket Connection            │
       │                                  │
       ▼                                  ▼
┌──────────────────────────────────────────────────┐
│         Flask-SocketIO Server                    │
│  ┌────────────────────────────────────────────┐  │
│  │      Event Handlers                        │  │
│  │  - on_connect                              │  │
│  │  - on_message                              │  │
│  │  - on_booking_update                       │  │
│  │  - on_disconnect                           │  │
│  └────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────┘

Flow:
1. Client 1 sends message
2. Server receives via WebSocket
3. Server broadcasts to Client 2
4. Client 2 receives in real-time
```

---

## 💳 Payment Flow (Paynow)

```
1. User books package
   │
   ▼
2. POST /api/bookings
   │
   ▼
3. Backend creates booking (pending)
   │
   ▼
4. Call Paynow API
   ├─ Integration ID
   ├─ Integration Key
   └─ Amount, Email, Phone
   │
   ▼
5. Paynow returns redirect URL
   │
   ▼
6. User redirected to Paynow
   │
   ▼
7. User completes payment
   │
   ▼
8. Paynow webhook → /api/payment/webhook
   │
   ▼
9. Backend updates booking (paid)
   │
   ▼
10. Send confirmation emails
```

---

## 🎯 Tech Stack Summary

```
┌─────────────────────────────────────────────────┐
│                  FRONTEND                       │
│  React 18 + Vite                                │
│  ├─ Routing: React Router v6                    │
│  ├─ Styling: Tailwind CSS                       │
│  ├─ State: TanStack Query + Context             │
│  ├─ Forms: React Hook Form                      │
│  └─ HTTP: Axios                                 │
└─────────────────────────────────────────────────┘
                      │
                      │ REST API + WebSocket
                      ▼
┌─────────────────────────────────────────────────┐
│                   BACKEND                       │
│  Flask 3.0 + Python 3.13                        │
│  ├─ ORM: SQLAlchemy                             │
│  ├─ Auth: JWT Extended                          │
│  ├─ Real-time: Flask-SocketIO                   │
│  ├─ Email: Flask-Mail                           │
│  └─ Payments: Paynow SDK                        │
└─────────────────────────────────────────────────┘
                      │
                      │ SQL
                      ▼
┌─────────────────────────────────────────────────┐
│                  DATABASE                       │
│  SQLite (Development)                           │
│  PostgreSQL (Production)                        │
│  ├─ 10 Models                                   │
│  ├─ Relationships                               │
│  └─ Migrations (Alembic)                        │
└─────────────────────────────────────────────────┘
```

---

## 🚀 Deployment Architecture (Future)

```
┌──────────────┐
│   CloudFlare │  ← CDN + SSL
│     (CDN)    │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│    Nginx     │  ← Reverse Proxy
│ Load Balancer│
└──────┬───────┘
       │
       ├──────────────┬──────────────┐
       ▼              ▼              ▼
┌───────────┐  ┌───────────┐  ┌───────────┐
│  Backend  │  │  Backend  │  │  Backend  │
│  Instance │  │  Instance │  │  Instance │
│   (Flask) │  │   (Flask) │  │   (Flask) │
└─────┬─────┘  └─────┬─────┘  └─────┬─────┘
      │              │              │
      └──────────────┴──────────────┘
                     │
                     ▼
              ┌─────────────┐
              │ PostgreSQL  │
              │  (Primary)  │
              └─────────────┘
```

---

## 💚 Summary

BantuBuzz uses a modern, scalable architecture:
- ✅ Separation of concerns (Frontend/Backend)
- ✅ RESTful API design
- ✅ JWT authentication
- ✅ Real-time capabilities
- ✅ Modular code structure
- ✅ Database relationships
- ✅ Payment integration
- ✅ Scalable design

---

*For more details, see the other documentation files!*
